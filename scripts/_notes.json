{"src/AdminPage.jsx":"• [high] CustomSelect dropdown (reusable hand-rolled select popup) (~lines 17-32) — status:handrolled; hand-rolled:dropdown/overlay\n  PLAN: Local CustomSelect component renders an absolutely-positioned (position:fixed via getBoundingClientRect) options panel with manual outside-click handling, z-index 9999. Replace entirely with FormKit Select (and MultiSelect where multi-value). It is used ~15 times across every modal in this file (region/city/manager/status/role/branch/account_type/is_primary/work_from/work_to/roleLevel). Removing it cascades: each consuming modal should use FormKit Select directly, which also gives FormKit's Dropdown/CalendarPopup styling. effort high because of the breadth of call sites.\n• [low] DeletePopup (delete confirmation dialog) (~lines 34-49) — status:handrolled; hand-rolled:overlay/header/confirm/footer\n  PLAN: Own fixed overlay (rgba+blur, z-index 1001), custom red top accent bar, trash icon circle, title/subtitle, and two raw <button>s. Replace the entire component with FormKit ConfirmDialog (variant 'delete'), passing itemName/onConfirm/onCancel. Invoked at line 1577 via {delTarget && <DeletePopup .../>}. Single swap point.\n• [high] Employee detail side panel (view user: data/tasks/attendance/logins tabs) (~lines 667-751) — status:handrolled; hand-rolled:overlay/header/fields/footer/dropdown\n  PLAN: Fixed overlay (z-index 999) with custom 920x650 shell, custom header (avatar+badges+edit/close buttons), left vertical tab rail, and 4 tab bodies built from raw divs/tables and the IB info-box helper. Wrap in FormKit Modal using its multi-page 'pages' API for the 4 tabs; replace header with Modal's Icon+title+close; use ModalSection/KCard for the grouped sections (SH3 helper) and ActionButton for the footer edit action. Read-only info rows (IB) can stay as content inside ModalSection. effort high (large, tabbed, data-dense, includes a table).\n• [high] View branch popup (info/accounts/staff tabs + edit/close footer) (~lines 753-819) — status:handrolled; hand-rolled:overlay/header/fields/footer\n  PLAN: Fixed overlay (z-index 1000), GLASS shell, custom color accent bar, custom header (avatar+names+close), 3 in-modal tabs, key/value rows + embedded Google Maps iframe, custom footer with bS edit button + close button. Wrap in FormKit Modal with 'pages' for the 3 tabs; use Modal header (variant edit/view) + ModalSection/KCard for each section; replace footer buttons with ActionButton. The maps iframe stays as custom body content. effort medium-high.\n• [high] Add/Edit branch popup (3-step wizard: basic/details/review) (~lines 821-897) — status:handrolled; hand-rolled:overlay/header/fields/footer/dropdown/calendar/stepper\n  PLAN: Fixed overlay (z-index 1000), GLASS shell, gold accent bar, fully custom 3-dot stepper, and body with raw <input>/<textarea> (fS), CustomSelect dropdowns, native type=color picker, custom day-toggle chips, and custom code/mobile prefix input groups; custom Next/Back/Save footer. Migrate to FormKit Modal with 'pages'=['البيانات الأساسية','التفاصيل','مراجعة'] (Modal's built-in stepper). Replace inputs: TextField (name_ar/name_en/email/google_maps_url), TextArea (address/notes), Select for region/city/manager/status, CurrencyField for opening_balance/daily_cash_limit, PhoneField for mobile, time selects via Select, day chips via MultiSelect or Segmented, color via a FormKit field if available else keep. Footer -> ActionButton. effort high.\n• [medium] Edit user popup (~lines 899-918) — status:handrolled; hand-rolled:overlay/header/fields/footer/dropdown\n  PLAN: Fixed overlay (z-index 1000), GLASS shell, gold accent, custom header/close, grid of raw <input>s for name_ar/name_en/email/phone/id_number/nationality, CustomSelect for role/branch/status, raw textarea for notes, custom Save/Cancel footer. Migrate to FormKit Modal (variant edit): TextField for text fields, PhoneField for phone, IdField for id_number, Select for role/branch/status, TextArea for notes, ActionButton footer. effort medium.\n• [medium] Add/Edit role popup (simple) (~lines 920-938) — status:handrolled; hand-rolled:overlay/header/fields/footer/dropdown\n  PLAN: Fixed overlay (z-index 1000), GLASS shell, custom header/close, raw inputs for name_ar/name_en, native type=color, CustomSelect status, textarea description, custom Save/Cancel footer. Migrate to FormKit Modal: TextField x2, Select for status, TextArea description, color via FormKit field if present, ActionButton footer. effort low-medium.\n• [medium] Permissions popup (per-role permission picker with module groups) (~lines 940-970) — status:handrolled; hand-rolled:overlay/header/fields/footer/checkbox\n  PLAN: Fixed overlay (z-index 1000), GLASS shell, blue accent, custom header/subtitle/close, body of hand-rolled module group headers + per-permission custom checkbox rows (raw divs with SVG ticks), custom Save/Cancel footer. Migrate to FormKit Modal; replace the custom checkbox rows with FormKit Checkbox (or a MultiSelect / grouped list using ModalSection per module); footer -> ActionButton (save in blue variant). effort medium.\n• [medium] Add/Edit bank account popup (~lines 972-995) — status:handrolled; hand-rolled:overlay/header/fields/footer/dropdown\n  PLAN: Fixed overlay (z-index 1000), GLASS shell, blue accent, custom header/close, raw inputs for bank_name/account_name/account_number/iban/swift_code, CustomSelect for account_type/branch/is_primary/is_active, textarea notes, custom Save footer. Migrate to FormKit Modal: TextField for bank/account fields, IdField/TextField for IBAN+SWIFT, Select for account_type/branch, YesNo or Switch for is_primary, Select/Switch for is_active, TextArea notes, ActionButton footer. effort medium.\n• [high] Review pending user popup (assign roles + activate) (~lines 999-1106) — status:handrolled; hand-rolled:overlay/header/fields/footer/dropdown\n  PLAN: Fixed overlay (z-index 1000), custom #1a1a1a shell, custom header (shield icon + title), user info card, hand-rolled role multi-select (chip tray + search input + card grid with custom checkboxes), CustomSelect branch, custom toggle switch (activateNow), raw textarea, custom Reject/Approve footer. Migrate to FormKit Modal: role picker -> MultiSelect, branch -> Select, activateNow -> Switch, notes -> TextArea, footer -> ActionButton (success variant). effort high (rich bespoke role-picker UI).\n• [high] Edit active user popup (branch reassign + deactivate trigger) (~lines 1108-1230) — status:handrolled; hand-rolled:overlay/header/fields/footer/dropdown\n  PLAN: Fixed overlay (z-index 1000), custom #1a1a1a shell, custom header/close, info cards, CustomSelect/editBranch field, relative-time stat boxes, and a red 'تعطيل' button that opens the deactivate popup; custom Cancel footer. Migrate to FormKit Modal (variant edit): Select for branch, ActionButton for footer + the deactivate trigger (delete-variant button). Read-only stat cards become ModalSection/KCard content. effort medium-high.\n• [high] Deactivate user popup (reason + type-name confirm) (~lines 1232-1310) — status:handrolled; hand-rolled:overlay/header/fields/footer/confirm\n  PLAN: Fixed overlay (z-index 1100, stacks over edit_active), custom red shell, warning panels, custom radio-group reason selector (raw divs), textarea notes, and a type-the-name confirmation input gating the destructive action; custom Deactivate/Cancel footer. Migrate to FormKit Modal (variant delete) or ConfirmDialog with body: RadioGroup for reason, TextArea for notes, TextField for the confirm-name input; ActionButton (delete) disabled until canSubmit. effort high (destructive confirm flow with bespoke validation).\n• [high] Reactivate user popup (saved roles + notify toggle) (~lines 1312-1395) — status:handrolled; hand-rolled:overlay/header/fields/footer\n  PLAN: Fixed overlay (z-index 1100), custom green shell, custom header/close, info cards (prev deactivation grid), saved-roles chip tray, custom toggle switch (reactNotify), info note, custom Activate/Cancel footer. Migrate to FormKit Modal (variant create/success-tinted): reactNotify -> Switch, footer -> ActionButton (success). Info/role-chip blocks become ModalSection content. effort medium-high.\n• [high] Add role full popup (3-step wizard: info/appearance+scope/permissions) (~lines 1397-1574) — status:handrolled; hand-rolled:overlay/header/fields/footer/dropdown/stepper/checkbox\n  PLAN: Fixed overlay (z-index 1000), custom #1a1a1a shell, injected <style> block for nav buttons, custom 3-segment progress bar, fieldset-style bordered sections, raw inputs (name_ar/name_en/code/description), CustomSelect roleLevel, color swatch grid, scope radio cards, two custom toggle switches (assignable/escalation), module-tab permission picker with custom checkbox grid + search; custom Back/Next/Create footer. Migrate to FormKit Modal with 'pages' (Modal stepper): Step1 TextField x3 + Select level + TextArea; Step2 ModalSection cards with color field, RadioGroup/Segmented for scope, Switch x2; Step3 search TextField + grouped Checkbox/MultiSelect permission picker. Footer -> ActionButton. effort high (largest, most bespoke modal in file).","src/App.jsx":"• [low] Forgot Password (ID + reset link, with success view) — DEAD CODE, gated behind {false && ...} (~lines 280-327) — status:handrolled; hand-rolled:overlay/header/fields/footer/success\n  PLAN: Currently disabled by `{false&&(()=>{...})()}` so it never renders. Either delete entirely, or rebuild from FKModal: overlay+header -> Modal (variant 'edit', Icon=lock, title); the ID input -> IdField; footer Send button -> ActionButton; the forgotSent success branch -> Modal `success` prop / SuccessView. Custom .fg-* CSS and inline fieldset go away.\n• [high] Register / New Account (single-step signup form + post-submit instructions view) (~lines 329-487) — status:handrolled; hand-rolled:overlay/header/fields/footer/success/dropdown\n  PLAN: Replace fixed overlay + var(--modal-bg) panel + .reg-* CSS with FKModal (variant 'add'). Fieldset 'Account Info' -> ModalSection/KCard. Fields: NameField->TextField, NatField (hand-built dropdown w/ search at 345)->Select or MultiSelect (with search), IdField->IdField, BranchField (hand-built dropdown at 363)->Select, PhoneField (+966 group)->PhoneField, EmailField->TextField(email), PwField/ConfirmField (with strength bars + eye toggle)->TextField type=password (keep strength meter as custom child or via Field slot). Footer Register button->ActionButton. regDone success branch->Modal success view. 2-col grid->GRID token. Drops the two hand-rolled nat/branch dropdown overlays.\n• [high] GM Initial Setup (SetupPage modal form + activation-sent view) (~lines 548-612) — status:handrolled; hand-rolled:overlay/header/fields/footer/success\n  PLAN: Near-identical structure to Register. Replace overlay+panel+.setup-* CSS with FKModal (variant 'add'/'edit'). Name->TextField, National ID->IdField, Email->TextField(email), Phone(+966)->PhoneField, Password/Confirm->TextField(password) keeping strength bars. Fieldset->ModalSection. Footer Register->ActionButton. `done` branch->Modal success view. Grid->GRID token.\n• [low] Reset Password success overlay (ResetPage 'done' state — full-screen fixed confirmation) (~lines 625-630) — status:handrolled; hand-rolled:overlay/success\n  PLAN: The `done` branch is a fixed inset:0 full-screen success panel. Replace with FormKit SuccessScreen (full-screen variant) or a FKModal in `success` mode with a 'Sign In' ActionButton. The non-done branch (632-645) is a centered card page, not an overlay, so leave it or convert its inputs to FormKit TextField(password) for consistency.\n• [low] Service Request launcher (wraps external ServiceRequestPage) (~lines 1231-1233) — status:partial; hand-rolled:fields\n  PLAN: Shell is already FKModal (open/onClose/hideHeader/height/width/scroll) — compliant. Inner content is the imported ServiceRequestPage component (separate file ./ServiceRequestPage.jsx); whether its internals use FormKit fields must be audited in that file, not here. Uses hideHeader, so the page provides its own header — consider moving title/Icon into the Modal header for consistency.\n• [low] Toast notification (success/error/delete floating pill) (~lines 1234) — status:handrolled; hand-rolled:overlay\n  PLAN: Hand-built fixed-position toast with inline keyframes. Not a Modal per se but a floating overlay. FormKit has no toast export listed; if a Flag/toast primitive is added to FormKit it should be centralized there. For now low priority — either leave, or extract a shared Toast into FormKit. Color/variant logic keys off Arabic substring 'خطأ'/'حذف' which is brittle.\n• [high] Profile modal (avatar upload, locked identity fields, editable phone/lang) + nested Email-change confirm overlay (~lines 1268-1378 (nested confirm 1355-1376)) — status:handrolled; hand-rolled:overlay/header/fields/footer/confirm\n  PLAN: Outer: fixed overlay + var(--modal-bg) panel + .pf-* CSS -> FKModal (variant 'edit') with header Icon=avatar+role title. Two fieldsets ('Profile' locked, 'Editable Information') -> ModalSection/KCard. Locked inputs (name_ar/name_en/id/nationality) -> TextField readOnly/disabled. Phone(+966)->PhoneField. Language picker (2 flag buttons)->Segmented or RadioGroup. Save footer->ActionButton. Avatar uploader -> FileField (or keep custom). Nested emailConfirmStep overlay (absolute inset:0 in-modal) -> FormKit ConfirmDialog with from/to summary and Confirm/Back ActionButtons.\n• [medium] CrudPage — View Row detail modal (read-only field grid, Edit/Close) (~lines 1489-1518) — status:handrolled; hand-rolled:overlay/header/fields/footer\n  PLAN: Replace fixed overlay + var(--sf) panel with FKModal (variant 'edit' or a read-only variant). Header avatar+title+Edit/Close buttons -> Modal header + ActionButton(s). Body field grid (flds.filter map) -> ModalSection containing read-only TextFields, or a dedicated FormKit read-view; Badge values already use local B/<Badge>. Use GRID token for 2-col layout.\n• [medium] CrudPage — Add/Edit record modal (generic field renderer: select/date/textarea/text) (~lines 1521-1540) — status:handrolled; hand-rolled:overlay/header/fields/footer\n  PLAN: Generic CRUD form. Replace overlay+panel with FKModal (variant 'add'/'edit' by pop value). Map field types: f.o(select)->Select, f.t==='date'->DateField, f.w(textarea)->TextArea, else->TextField (use NumberField/CurrencyField when f.k includes amount/salary/capital). Footer Save/Cancel->ActionButton. Native <select>/<input> with local fS style get removed. This is high-leverage: one migration covers installments+expenses pages.\n• [high] TransferCalcPage — New/Edit Transfer Quote wizard (multi-step: worker, costs, etc.) (~lines 2502-2625) — status:handrolled; hand-rolled:overlay/header/fields/footer/calendar/dropdown\n  PLAN: Largest hand-rolled modal. Replace overlay+88vh panel with FKModal using `pages` (multi-page) API for the wizard steps instead of the custom steps bar + wizStep state. Header Save/close->ActionButton. Local field helpers LBL/INP/SEL/TOG -> TextField/Select/Segmented(YesNo). Existing/New worker toggle->Segmented. Footer Back/Next/Cancel->ActionButton via Modal footer. Any date inputs->DateField, money inputs->CurrencyField. Note: the advanced-filter panel (2051) and office dropdown and detailsRow detail (2207) are inline page sections, NOT overlays, so excluded from modal count; the advanced filter already uses FormKit-imported DateField/Sel from KafalaCalculator (not FormKit.jsx).\n• [medium] AppointmentsPage — New/Edit Appointment modal (title/date/time/type/status/location/notes + delete) (~lines 2681-2700) — status:handrolled; hand-rolled:overlay/header/fields/footer\n  PLAN: Replace overlay+var(--sf) panel with FKModal (variant by pop==='new'). Title->TextField, Date type=date->DateField, Time type=time->TextField(time) (no FormKit time field listed; keep native or extend FormKit), Type/Status <select>->Select, Location->TextField, Notes textarea->TextArea. Footer Save + Delete->ActionButton (Delete as delete-variant). Wrap fields in ModalSection.\n• [medium] OpExpensesPage — New/Edit Expense modal (amount/category/description/date/vendor) (~lines 2766-2781) — status:handrolled; hand-rolled:overlay/header/fields/footer\n  PLAN: Replace overlay+var(--sf) panel with FKModal (variant by pop==='new'). Amount type=number->CurrencyField/NumberField, Category <select>->Select, Description->TextField, Date->DateField, Vendor->TextField. Footer Save->ActionButton. Wrap in ModalSection; use GRID for 2-col rows. Missing payment_method/is_recurring fields from state could become Select/Switch.","src/BankAccountsPage.jsx":"• [medium] LinkedOfficesEditor — manage offices linked to a bank account + their purpose (~lines 178-272) — status:handrolled; hand-rolled:overlay/header/fields/footer/dropdown\n  PLAN: Defined locally in THIS file and entirely custom: own fixed overlay (line 216: position:fixed, rgba(0,0,0,.7), backdropFilter blur, zIndex 1000), own header with Wallet icon + title + a rotated-Plus close button (lines 218-225), a hand-rolled native <select> styled via selStyle (lines 254-257), the custom PurposeChips toggle component, and a custom gold 'ربط المكتب' submit button (lines 263-265). Migrate: wrap in FormKit Modal (variant='edit', Icon=Wallet, title 'المكاتب المرتبطة') so overlay+header+close come for free; replace the native <select> with FormKit Select (searchable, opts from available branches); keep PurposeChips inside a ModalSection/KCard or replace with FormKit MultiSelect/Segmented; replace the gold add button with ActionButton; wrap each existing-link row in a ModalSection. NOTE: this component appears unused/dead — the edit flow (openEdit at line 617) now manages office links inline inside BankAccountFormModal, per the comment at lines 620-621 ('it replaces the old standalone linked-offices editor'). Confirm it's truly orphaned; if so, delete rather than migrate.\n• [high] BankAccountFormModal — add/edit/link bank account (imported from BranchesPage.jsx, rendered here at 706-710 / 458-462 path via openAdd & openEdit) (~lines 706-710 (render site); impl in BranchesPage.jsx 1454-1838) — status:partial; hand-rolled:fields/success\n  PLAN: Imported component, not defined in this file — render site is line 706-710 (the `modal` const), opened via openAdd (616) and openEdit (617). Its form body IS wrapped in FormKit Modal (BranchesPage line 1653: <FKModal open onClose accent width=600 scroll title Icon footer errorMsg>), which is good. But two parts are hand-rolled: (1) the SUCCESS view is a separate hand-rolled ReactDOM.createPortal fixed overlay (BranchesPage lines 1558-1573, with its own rgba(0,0,0,.7)+blur backdrop, fixed height BANK_MODAL_H, and custom check/ring keyframe animations) instead of using FormKit's Modal `success` prop + SuccessView; (2) inner fields are custom — CustomSel for office multi-select, raw <input> using kafalaInput/sF styles for bank/account/iban/swift, and a hand-built search-results list for link mode (BranchesPage 1666-1838). Migrate in BranchesPage: pass the success card via FKModal's `success` prop using SuccessView (it supports compareRows — the edit-diff cmpRows already match that shape) and drop the createPortal block; swap CustomSel→FormKit Select/MultiSelect, raw inputs→TextField/IdField/IbanField, the IBAN drop zone→FileField, expiry→DateField, is_primary→Switch/YesNo. This file itself needs no change beyond benefiting from the upstream fix.\n• [medium] BankCardModal — add/edit a bank card (imported from BranchesPage.jsx, rendered at 458-462 from BankAccountDetailPage) (~lines 458-462 (render site); impl in BranchesPage.jsx 2154-2225) — status:partial; hand-rolled:fields/success\n  PLAN: Imported component opened from the card section via setCardModal({open,card}) at lines 387 & 395, rendered at 458-462. Form body IS wrapped in FormKit Modal (BranchesPage line 2196: <FKModal open onClose accent width=560 scroll>), good. Hand-rolled parts: (1) SUCCESS view is a hand-rolled ReactDOM.createPortal fixed overlay (BranchesPage line 2180) rather than FKModal's `success` prop/SuccessView; (2) card fields (card_number, holder_name, pin, cvv, expiry) appear to be custom inputs inside the FKModal body. Migrate in BranchesPage: route success through FKModal `success`=<SuccessView/>; replace custom card inputs with FormKit TextField/NumberField (PIN/CVV) and DateField (expiry). No change required in THIS file.","src/BranchesPage.jsx":"• [low] Branch add/edit form modal (BranchFormModal) (~lines 2423-2556) — status:partial; hand-rolled:fields (one)\n  PLAN: Closest to compliant. Shell=FKModal, body=FKSection, region/city/district=FKSelect, success=FormKit SuccessView, footer=FKAction. The only hand-rolled part is the 'كود المكتب' field (lines 2513-2551): a custom read-only display + pencil toggle that swaps to a raw <input> styled with sF. Replace with FormKit TextField (uppercase/alnum transform + dir=ltr mono styling via its props), or keep the auto-gen-with-override pattern as a small wrapper but use TextField for the editable state instead of the raw <input>+sF.\n• [high] Bank account add/link/edit form modal (BankAccountFormModal) — form state (~lines 1652-1839) — status:partial; hand-rolled:fields/footer-button/dropdown/calendar/filefield\n  PLAN: Shell is FKModal (good), but the entire body is hand-rolled. Replace: footer SaveBtn (RoleUI) -> FormKit ActionButton; CustomSel for bank/office (lines 1671-1673,1781-1788) -> FormKit Select/MultiSelect; PurposeMultiSelect (1762,1815) -> FormKit MultiSelect with the PURPOSE_OPTS list; raw <input> for account_name/account_number/iban (1789-1808) -> TextField/IdField (iban as IdField/TextField with uppercase); DateField from KafalaCalculator (1811) -> FormKit DateField; custom IBAN drag-drop <label>+<input type=file> (1819-1831) -> FormKit FileField; the bordered 'بيانات الحساب' frame -> FormKit ModalSection/KCard; the link-mode search results list is custom (keep as body content but wrap labels via FormKit). Also wrap field groups in ModalSection instead of the hand-built bordered fieldset.\n• [medium] Bank account modal SUCCESS overlay (BankAccountFormModal success state) (~lines 1524-1650) — status:handrolled; hand-rolled:overlay/header/close/success/animation/compare-rows\n  PLAN: Entirely custom: own ReactDOM.createPortal fixed overlay (rgba backdrop+blur), own X close button, own SVG check ring animation (brsCheck/brsRingDraw keyframes), own before->after compare-row grid and own detail-row summary. Replace wholesale with FormKit Modal's built-in `success` prop fed by FormKit SuccessView (it supports title, code/codeSub, and compareRows — already used by BranchFormModal). Render success INSIDE the same FKModal instead of returning a separate portal, eliminating the duplicate overlay/size-matching logic (BANK_MODAL_H).\n• [medium] Bank card add/edit form modal (BankCardModal) — form state (~lines 2195-2225) — status:partial; hand-rolled:fields/footer-button\n  PLAN: Shell is FKModal (good). Replace footer SaveBtn (RoleUI) -> FormKit ActionButton; the bordered 'بيانات البطاقة' fieldset -> FormKit ModalSection/KCard; the four raw <input>s (card_number/holder_name/pin/cvv, lines 2209-2221) -> FormKit TextField (mono/dir=ltr variants) or NumberField for pin/cvv; the lbl style object -> rely on field label props.\n• [low] Bank card modal SUCCESS overlay (BankCardModal done state) (~lines 2179-2193) — status:handrolled; hand-rolled:overlay/header/close/success\n  PLAN: Custom ReactDOM.createPortal fixed overlay with its own backdrop, own X close button and own check-circle SVG. Replace with FormKit Modal `success` prop + SuccessView rendered inside the same FKModal, dropping the standalone portal.\n• [medium] CustomSel hand-rolled dropdown (generic select used across bank modals) (~lines 86-204) — status:handrolled; hand-rolled:dropdown\n  PLAN: Hand-built floating combobox: button trigger + ReactDOM.createPortal panel (fixed positioned, getBoundingClientRect), own search box, own single/multi option rows with check marks. This is a from-scratch reimplementation of FormKit Select/MultiSelect (which already provide searchable + multi). Delete CustomSel and use FormKit Select (single) / MultiSelect (multi) everywhere it's referenced (BankAccountFormModal office + bank pickers).\n• [medium] PurposeMultiSelect hand-rolled dropdown (account purpose picker) (~lines 213-269) — status:handrolled; hand-rolled:dropdown\n  PLAN: Another hand-built portal multi-select (button + fixed-positioned createPortal panel + icon/checkbox rows) operating on a ' · '-joined string. Replace with FormKit MultiSelect fed by PURPOSE_OPTS, converting between array and the ' · ' joined-string at the boundary (or store as array). Removes the second bespoke dropdown implementation.","src/FacilitiesPage.jsx":"• [medium] Add Facility modal (manual create form: Arabic/English name, unified number, GOSI number, HRSD number) (~lines 6896-6949) — status:partial; hand-rolled:fields, footer, body section wrapper\n  PLAN: Shell is correct (FKModal with open/onClose/accent/width/scroll/title/Icon). Everything inside is hand-rolled and must be swapped: (1) The footer is a custom `<button className='fac-add-btn'>` with an inline <style> block and a hand-built spinner — replace with FormKit ActionButton (variant=create/add, loading=adding, label Save/Saving). (2) The bordered 'Facility Data' section is a hand-built fieldset div with an absolutely-positioned floating legend (lines 6911-6917) — replace with FormKit ModalSection/KCard (title + Icon). (3) All five inputs are raw <input> elements with a local `sF` style object and a local `Lbl` label component (lines 6919-6943): name_ar/name_en -> TextField (with rtl/ltr direction), unified_number (digit-strip, maxLength 10) -> NumberField or IdField, gosi_number (digit-strip, maxLength 12) -> NumberField/IdField, hrsd_number -> TextField. Use FormKit GRID for the 2-col grid (gridTemplateColumns '1fr 1fr') and the required-asterisk handling that TextField/`req` already provides. Remove the inline <style> tag and local sF/Lbl once fields are FormKit.\n• [low] Platform view dropdown menu (_PlatformDropdown — selects GOSI/Qiwa/Muqeem/etc. table lens; toggle button + floating option list) (~lines 1764-1818) — status:handrolled; hand-rolled:dropdown, overlay, trigger button, option list\n  PLAN: Fully custom: local `open` useState, a custom trigger <button> with chevron, and an absolutely-positioned floating menu div (position:absolute, top:calc(100%+4px), zIndex:10, custom shadow/border, lines 1786-1812) listing PLATFORMS with brand dot + label + subtitle + active check. Replace the entire floating menu with FormKit Dropdown (anchored popup with options), or if it is purely a value selector, replace the whole control with FormKit Select / Segmented driven by tableView/setTableView. Note there is no outside-click/escape close handler currently, which FormKit Dropdown provides for free.","src/InvoicePage.jsx":"• [high] ActionModal — payment / refund / cancel / print multi-step modal (the only real floating modal) (~lines 1587-2083) — status:partial; hand-rolled:fields, sections\n  PLAN: Shell is correct: wraps FormKit <Modal open onClose title Icon accent width onSubmit submitting submitLabel nextLabel backLabel pages /> and feeds success via FormKit <SuccessView> (good). Everything INSIDE the pages is hand-rolled and must be swapped to FormKit fields. (1) The local fieldset/legend bordered boxes (defined lines 1691-1692, reused across invoiceInfo/printOptions and in the child forms) should become FormKit ModalSection/KCard (titled bordered section). (2) invoiceInfo (1959-1999) summary rows are bespoke flex rows — fine to keep as a read-only ModalSection body, or rebuild with KCard. (3) printOptions (2002-2019) uses raw <input type=checkbox> — replace each with FormKit Checkbox/Switch inside a ModalSection. (4) Child form components below carry the bulk of the hand-rolled fields (see PaymentDetailsForm/RefundDetailsForm/RefundReasonForm/CancelReasonForm notes). No FK fields used yet inside any page.\n• [high] PaymentDetailsForm — amount + method toggle + bank-transfer ref/receipt + bank-account picker (rendered as ActionModal payment page 2) (~lines 1132-1293) — status:partial; hand-rolled:fields\n  PLAN: Hand-rolled amount input with custom fmtAmt/unfmtAmt thousands formatting + clamp -> replace with FormKit CurrencyField (label, unit 'ريال', value/onChange; it already formats). The cash/bank toggle (1188-1201) -> FormKit Segmented (options cash/bank) or RadioGroup. Transfer reference raw <input> (1211-1217) -> FormKit TextField. The drag-drop receipt <label>+hidden <input type=file> (1218-1232) -> FormKit FileField (accept image/pdf, value/onChange). The bank-account search <input> + filtered selectable card list (1237-1287) -> FormKit Select/Dropdown (searchable, getLabel/getSub/renderCell) over bankAccounts so selection is one FK component. Wrap groups in ModalSection instead of the local fieldset/legend.\n• [high] RefundDetailsForm — refund amount + method toggle + bank ref/receipt + bank-account picker (ActionModal refund page 2) (~lines 1295-1443) — status:partial; hand-rolled:fields\n  PLAN: Near-identical twin of PaymentDetailsForm. Refund amount input -> CurrencyField; method toggle (1339-1352) -> Segmented; transfer ref (1362-1368) -> TextField; drag-drop receipt (1369-1383) -> FileField; bank-account search + card picker (1387-1436) -> searchable Select/Dropdown. Consolidate with PaymentDetailsForm into one shared FormKit-based AmountMethodBankForm to remove duplication. Replace fieldset/legend with ModalSection.\n• [medium] RefundReasonForm + CancelReasonForm — reason dropdown / notes / cancel-reason textarea (ActionModal refund page 3 and cancel page 2) (~lines 1507-1585) — status:partial; hand-rolled:fields, dropdown\n  PLAN: RefundReasonForm uses the local hand-rolled ModalDropdown (portal dropdown, lines 1445-1505) for the reason picker -> replace with FormKit Select (label, options from lookup_items, placeholder, disabled). Both forms use raw <textarea> (1550-1556 and 1575-1581) -> FormKit TextArea (label, rows, value/onChange). The CancelReasonForm warning banner (1569-1572) can stay as custom markup or move into a ModalSection hint. Replace fieldset/legend wrappers with ModalSection. NOTE: the standalone ModalDropdown component (1445-1505) is the hand-rolled dropdown to delete once Select replaces it.","src/KPIPage.jsx":"• [low] Set Targets editor (per-metric monthly target values) (~lines 346-377) — status:partial; hand-rolled:fields, footer\n  PLAN: Modal shell is already FormKit (FKModal with title/subtitle/Icon/accent/width/scroll/footer). Two parts are hand-rolled. (1) Footer: replace the two raw <button> elements (Cancel + Save Targets, lines 350-357) with FormKit ActionButton — a neutral/ghost variant for Cancel calling setEditPop(false), and a primary variant (variant create/gold accent) for Save with loading state driven by `saving`. (2) Body fields: the METRICS.map loop (lines 360-374) renders a hand-built label row plus a raw <input type=\"number\"> styled by the inline `fS` object. Replace each with a FormKit field bound to editForm[m.key]: use CurrencyField for metrics where m.unit==='currency' (revenue, collection, expenses_limit) and NumberField for the rest (transactions_completed, new_clients, invoices_issued), passing label={m.l}, value, onChange. Drop the `fS` style object and the custom colored-dot label markup; FormKit field label + the metric color can be conveyed via the field's label/accent. Optionally wrap the list in a ModalSection/KCard for a titled bordered group. No success view exists; could add Modal `success` if desired but not required.","src/ServiceAdminPage.jsx":"• [medium] Date picker calendar popup (CalendarPopup) + its full-screen click-catcher backdrop in DateInput (~lines 42-138) — status:handrolled; hand-rolled:overlay/calendar/dropdown\n  PLAN: Entirely custom floating calendar built with ReactDOM.createPortal to document.body, own fixed positioning (openUp/spaceBelow math), own month grid, own today/clear footer, and a separate full-screen fixed backdrop (DateInput line 133, position:fixed inset:0 zIndex:2000) that closes it. FormKit already exports CalendarPopup and a DateField. Replace the whole CalendarPopup+DateInput pair with FormKit's DateField (which internally renders FormKit's CalendarPopup/Flag and handles anchoring + outside-click). This kills the bespoke portal, the anchor measuring, the YYYY-MM-DD typing mask, and the backdrop. Used only for the workPermitCutoffDate field (f.t==='date') inside renderField (line 677); swapping renderField's date branch to FormKit DateField removes both helper components.\n• [low] Per-branch override editor modal (dead code — gated behind `false &&`, never renders) (~lines 1862-1992) — status:handrolled; hand-rolled:overlay/header/fields/footer\n  PLAN: Hand-rolled modal shell: header with icon box + title 'تخصيص لمكتب' + custom close X button (line 1891), body with branch-picker grid, two custom Toggle switches, and a pricing-fields grid of raw <input> elements (line 1959), plus a footer with custom delete/cancel/save buttons (lines 1971-1988). It is permanently disabled (`{false&&overrideEditor&&...}` at line 1863) because the live override editor was moved inline into the section card (renderInlineOverrideEditor, lines 416-554). Cleanest path: delete this dead block entirely. If it were ever revived, rebuild as a FormKit Modal (variant 'edit') with ModalSection for the branch picker, Switch fields for active/billable, NumberField/CurrencyField for pricing, and ActionButton footer. Recommend deletion over migration.","src/ServiceRequestPage.jsx":"• [high] Main 6-step service-request wizard (modal body: header/close/progress, content, footer) (~lines 1402-4239) — status:handrolled; hand-rolled:overlay/header/fields/footer\n  PLAN: Entire wizard is the body of a modal whose overlay/shell is supplied by the parent caller (renders height:100% flex column, no fixed backdrop here). Wrap the whole thing in FormKit Modal with variant 'add' and the 6-step 'pages' API instead of the manual step/displayStep state + custom progress-bar dots (lines 1452-1454) and custom .sr-next-btn/.sr-back-btn footer (4212-4237) — replace those with Modal's built-in footer/ActionButton and page navigation. Replace the hand-built header (icon+title+close at 1442-1451) with Modal's Icon+title+close props. The dozens of hand-built fields use a recurring pattern: a relative bordered box with an absolutely-positioned legend at top:-9 (the 'const legend=...' / 'top:-9,right:14' blocks, e.g. 1658, 2256, 2348, 3204, 3330, 3431, 3507) and inline-styled inputs (regInpS at 1656/4126, roBox readonly boxes at 2342/2430/2558 etc). Map each: legend+text input -> TextField; legend+number input (amounts, paidAmount at 3965-3970, total at 3638-3644) -> NumberField/CurrencyField; phone inputs -> PhoneField; id/iqama -> IdField; the segmented toggles (display:flex gap pills, e.g. 2184, 2734, payment-type/method) -> Segmented; the in-modal sections/legend boxes -> ModalSection/KCard; file upload dropzones (transferReceipt at 4001-4007, 4185-4191) -> FileField; the notes textareas -> TextArea; YesNo/Switch toggles (addAdminNote/addClientNote/hasBroker/workerIsClient) -> Switch/YesNo. The broker fieldset (4057-4067) and group fieldsets (2140) -> ModalSection. Very large effort due to ~2800 lines of bespoke per-service field layouts.\n• [medium] Post-submit success screen (check + invoice number + total) (~lines 1377-1401) — status:handrolled; hand-rolled:success\n  PLAN: Custom success view (green check circle, title, invoice-number row with copy button, total row, 'تم' button) rendered by early-returning before the main body. Replace with FormKit Modal's built-in 'success' view or SuccessView/SuccessScreen, passing invoice number + total as the detail rows and wiring the dismiss button to onClose. Once the wizard is a FormKit Modal, set its success state instead of this branch.\n• [medium] CompactDatePicker — hand-built fixed-position calendar popup (~lines 140-198) — status:handrolled; hand-rolled:calendar/dropdown\n  PLAN: Self-contained custom date picker: a trigger button plus a position:fixed gold/dark calendar panel (line 184) with manual viewport placement, AR month/day grids, outside-click handling. Replace the whole component with FormKit DateField (or CalendarPopup for the floating panel). Also note the file imports DateField from KafalaCalculator.jsx (line 4) rather than from FormKit — those usages should also be swapped to FormKit DateField. Delete AR_MONTHS/AR_DAYS local consts once migrated.\n• [medium] NiceSelect — hand-built fixed-position searchable dropdown (~lines 201-270) — status:handrolled; hand-rolled:dropdown\n  PLAN: Custom searchable select: trigger button + position:fixed panel (line 247) with auto-flip placement, search input, scrollable option list, check icon on selected. Replace with FormKit Select (single) / MultiSelect, or FormKit Dropdown for the floating list primitive. All call-sites currently passing {value,onChange,options} map directly to Select props.\n• [high] Inline nationality + bank/broker/quote selection dropdowns (client nat, broker nat, bank account, broker, kafala-quote pickers) (~lines 1659-1700, 3994-4054, 4074-4090, 4142-4156, 3200-3240) — status:handrolled; hand-rolled:dropdown\n  PLAN: Several hand-rolled selection popups: client nationality dropdown (natOpenClient/natPos), broker nationality dropdown (natOpenBroker/natPosBroker at 4142-4156 with its own position:fixed inset:0 scrim + position:fixed panel), plus bank-account / broker / kafala-quote searchable result lists rendered inline with search inputs and manual getBoundingClientRect positioning (openNatDropdown/openNatBrokerDropdown/openBankAccDropdown helpers at 290/317/332). Replace the nationality pickers with FormKit Select/Dropdown (with Flag for the flag rendering, natFlagUrl). Replace the bank-account/broker/kafala-quote searchable result panels with FormKit Select or a Dropdown+ScrollBox; the duplicated search-input + filtered-list logic collapses into Select's built-in search. Remove the manual fixed-position scrim divs (e.g. 4142) — FormKit Dropdown handles overlay/outside-click.","src/SettingsPage.jsx":"• [medium] CustomSelect — hand-rolled select dropdown (floating options panel, used inside FormPopup) (~lines 48-64) — status:handrolled; hand-rolled:dropdown/overlay/options-list\n  PLAN: Replace entirely with FormKit Select (single-value). It renders its own trigger (div styled like fS) plus a position:fixed portal panel (line 61) with manual getBoundingClientRect positioning, manual outside-click handler, and manual selected/checkmark rendering — all of which FormKit Select / Dropdown already provide. Swap CustomSelect usages in FormPopup (line 77) for FormKit Select with options mapped {value,label}. Once FormPopup is migrated to FormKit fields, this whole component can be deleted.\n• [medium] KSelect — second hand-rolled select dropdown (Kafala-styled, used inside OccupationFormPopup) (~lines 88-107) — status:handrolled; hand-rolled:dropdown/overlay/options-list\n  PLAN: Duplicate of CustomSelect with different cosmetic styling (kFS). Same pattern: position:fixed portal (line 100), manual rect positioning, manual outside-click, empty-state and checkmark rendering. Replace all KSelect usages (category/type/region/nationality pickers at lines 258,262,288,292) with FormKit Select. Delete component after OccupationFormPopup migration. Consolidating CustomSelect+KSelect into one FormKit Select removes ~40 lines of duplicated dropdown logic.\n• [medium] FormPopup — generic add/edit form modal (used for Documents 'doc' kind; legacy fallback for unmatched pop kinds) (~lines 66-82) — status:partial; hand-rolled:fields/footer\n  PLAN: Shell is FormKit (FKModal open/onClose/accent/width/scroll/title). Hand-rolled inside: (1) footer buttons (Cancel + Save) built as raw <button> styled with bS — replace with FormKit ActionButton x2 via Modal footer, or use Modal variant create/edit which provides standard footer. (2) The field grid is fully hand-rolled — raw <input> (line 79), <textarea> (line 78), a custom segmented button group for f.btn options (line 76), and CustomSelect for f.opts (line 77). Replace: plain text -> TextField; textarea (f.w) -> TextArea; opts+btn -> Segmented or YesNo; opts -> Select; numeric 'sort_order' -> NumberField. Wrap fields in a ModalSection/KCard. Field metadata already lives in popFields (lines 471-482) so this maps cleanly to a FormKit field-array render. Currently still reached for pop==='doc' (line 1029).\n• [high] OccupationFormPopup — unified add/edit modal for occupation/nationality/embassy/region/city/district/category/item/bank (~lines 84-354) — status:partial; hand-rolled:header/fields/footer/success/dropdown\n  PLAN: Uses FKModal but with hideHeader (line 211) and then re-implements everything by hand inside. Replace: (1) Custom header with icon tile + title + close X (lines 214-223) -> FormKit Modal header (drop hideHeader, pass title={form._id?L.edit:L.add}, Icon, and variant create/edit for accent color). (2) Bordered 'Info' fieldset with floating legend + context chips (lines 226-254) -> ModalSection / KCard with title. (3) All inputs are hand-rolled: KInp (raw <input>, line 87) -> TextField; the 'code/key' input with inline validation (lines 274-285) -> TextField (or IdField) with helper/error props; KSelect pickers -> Select; the Yes/No 'is_system' and 'is_active' button pairs via btn() helper (lines 210,315-316,326-327) -> YesNo or Segmented; flag_url input+preview (lines 304-309) -> TextField (or FileField) plus preview; sort_order (line 321) -> NumberField. (4) Footer Save button (lines 338-343) with the .occ-nav-btn custom CSS -> Modal footer + ActionButton. (5) Saved-flash success overlay (lines 344-351) -> FormKit Modal 'success' view / SuccessView. (6) KSelect dropdowns -> FormKit Select. This is the single biggest migration in the file — large because it is a config-driven multi-entity form; recommend driving it from a FormKit field schema keyed by 'kind'.","src/WorkforcePage.jsx":"• [medium] Add Worker (إضافة عامل) — manual worker insert form (~lines 693-757) — status:partial; hand-rolled:fields, footer, section\n  PLAN: Shell is correct — uses FKModal with open/onClose/accent/width/scroll/title/Icon props. Interior fully hand-rolled. (1) FOOTER: replace the custom `.wkr-add-btn` button + inline <style> + manual spinner with FormKit ActionButton (its loading/disabled props handle spinner + 'Saving…'). (2) SECTION: the bordered box with absolute-positioned 'بيانات العامل' legend (708-714) -> FormKit ModalSection/KCard with title+Icon. (3) FIELDS: IIFE at 715-754 defines local `sF` input style + `Lbl` label, then renders raw <input>s — name <input> (723) -> FormKit TextField; iqama-number <input> (727, manual /\\D/ strip + maxLength 10) -> FormKit IdField/NumberField; drop local Lbl for each field's label/required prop. (4) EXTERNAL FIELDS: DateField (730) and OccSelect (734,744) come from ./pages/KafalaCalculator.jsx, NOT FormKit — swap DateField -> FormKit DateField, the two OccSelect autocompletes -> FormKit Select (searchable nationalities/occupations). (5) Remove in-file F/C token redefinitions + wkr-spin/wkr-add-btn <style>; use Modal variant='add' instead of accent={C.gold}; optional SuccessView on save.","src/pages/DepositsPage.jsx":"• [high] CashDepositModal — record a standalone cash-drawer deposit (2-step wizard: amount/date/time, then card + slips) (~lines 742-854) — status:partial; hand-rolled:fields, success\n  PLAN: Shell is correct: FKModal with multi-page `pages`, accent, onSubmit/submitting/submitLabel/nextLabel/backLabel — keep this. But every inner field is hand-rolled or sourced from KafalaCalculator instead of FormKit: (1) the 'cash on hand' gold-legend fieldset should become a FormKit ModalSection/KCard. (2) the 'Amount deposited' raw <input> with manual withThousands()/regex cleaning + exceeds-validation should be FormKit CurrencyField (or NumberField) which handles thousands grouping and error state via the `error`/`hint` props. (3) `DateField` from KafalaCalculator -> FormKit DateField/CalendarPopup. (4) custom `TimeField` (lines 686-740) -> there is no FormKit time field listed; either add one to FormKit or keep but flag (currently fully hand-rolled, see separate entry). (5) `Sel` (KafalaCalculator) for the card selector with custom render -> FormKit Select (supports searchable/searchPlaceholder/option render). (6) `MultiFileField` (lines 626-659) -> FormKit FileField (multi). (7) The `kLbl`/`Lbl` and `fieldS` local style objects -> drop in favor of FormKit field labels/tokens (F, sF). (8) CRITICAL: the success state (lines 793-805) renders a COMPLETELY SEPARATE hand-rolled fixed overlay instead of FKModal — replace with FKModal's built-in `success` prop / SuccessView so the shell never unmounts. Also delete dead `overlayS`/`modalS` consts at 615-616 (unused).\n• [low] CashDepositModal success overlay — 'Deposit recorded / pending verification' confirmation (~lines 793-805) — status:handrolled; hand-rolled:overlay, success\n  PLAN: Entirely custom: own position:fixed overlay (rgba(0,0,0,.7)+blur), own panel box, hand-built green check circle, title and subtitle. This is rendered as an early-return that REPLACES the FKModal, so it bypasses the design system on the most visible moment. Replace with FormKit Modal's `success` view (pass `success` config to FKModal) or the standalone SuccessView/SuccessScreen export — supplying icon/title ('تم توثيق الإيداع')/subtitle ('في انتظار التحقق'). Removes ~13 lines of bespoke overlay code.\n• [medium] TimeField popup — hand-built time picker (AM/PM + hour + minute scroll columns, Now/Clear) used for deposit time (~lines 686-740 (popup portal at 708-738)) — status:handrolled; hand-rolled:overlay, dropdown, calendar\n  PLAN: Fully bespoke floating popup: own fixed click-catcher backdrop (z 1000) + fixed positioned panel (z 1001) computed from getBoundingClientRect with manual flip-up logic, three TimeCol scroll lists, and Now/Clear footer buttons — none from FormKit. FormKit exposes CalendarPopup and Dropdown but no dedicated time field in the listed exports. Recommended: add a FormKit TimeField (mirroring DateField/CalendarPopup positioning + Dropdown anchoring) and replace this, OR at minimum rebuild the floating panel on FormKit Dropdown so the overlay/anchor/flip logic is centralized. Until a FormKit time primitive exists this stays hand-rolled.","src/pages/KafalaCalculator.jsx":"• [high] Main Kafala transfer-calculator wizard (4-tab stepper: worker data / details / pricing / review, with hand-built header + progress bar + footer nav) (~lines 1105-1642, 1794-1795) — status:partial; hand-rolled:header, fields, sections, footer, success(inline issue flow)\n  PLAN: Shell is FKModal but opened with `hideHeader` + custom 720/640 sizing, so nearly everything inside is bespoke. (1) Drop hideHeader and pass Modal's title/subtitle/Icon (Tag) props + use Modal's built-in `pages` multi-step machinery instead of the manual `tab` state, the hand-built gold progress bar (1119-1123) and the .kc-nav-btn footer (1577-1642). (2) Replace every hand-rolled field: Iqama `Inp` (1160) -> IdField/TextField; DOB & iqamaExpiry local DateField (1179,1211) -> FormKit DateField; phone +966 group (1195-1201) -> PhoneField; occupation OccSelect (1208,1375) -> Select/Dropdown(searchable); HijriDate built from local `Sel` (1139-1151) -> 3x FormKit Select or DateField(calendar='hijri'); transfer/renewal RenewalPill rows (1331-1365) -> Segmented; changeProfession YesNo (1369) -> FormKit YesNo; extras name/amount inputs (1382-1384) -> TextField + CurrencyField; absher checkbox+input (1550-1560) and transfer inline input (1534) -> Checkbox + CurrencyField. (3) Replace the gold-bordered titled `KCard`/`Card`/`Group` panels (505-514,1152-1156,1228-1236) with FormKit ModalSection/KCard. (4) Footer Previous/Next/Issue buttons -> Modal `footer` with ActionButton (dir back/fwd, loading state). Large surface; many custom interactions.\n• [medium] CalendarPopup — hand-built dark date picker (fixed-position portal-less panel with month nav, today/clear, min-date) (~lines 154-206) — status:handrolled; hand-rolled:overlay, calendar\n  PLAN: Entirely custom floating calendar (own fixed positioning, flip-up logic, day grid, Today/Clear). Delete and use FormKit's exported CalendarPopup, or better, stop calling it directly and rely on FormKit DateField which embeds its own calendar. This component is also `export`ed, so check external importers before removing; ideally re-export FormKit's CalendarPopup for compatibility.\n• [medium] Local DateField calendar trigger + backdrop (wraps the hand-built CalendarPopup with its own fixed backdrop) (~lines 209-262 (popup branch 255-258); also legacy DateInp 129-144) — status:handrolled; hand-rolled:overlay, calendar, fields\n  PLAN: This whole local DateField (text mask + calendar icon button + fixed inset backdrop + CalendarPopup) duplicates FormKit DateField. Replace all usages (1179, 1211) with the imported FormKit DateField and delete this local export plus the dead `DateInp` helper (129-144) which is also a hand-rolled date overlay. FormKit DateField already provides label/req/min and the popup.\n• [medium] OccSelect — hand-built searchable occupation dropdown (portal, fixed-position, search box, scroll list) (~lines 264-327) — status:handrolled; hand-rolled:dropdown\n  PLAN: Custom createPortal dropdown with own button, search input, outside-click handling and ar/en rows. Replace with FormKit Dropdown (searchable) / Select, mapping occupations to {key,label,sub:name_en}; FormKit Dropdown already supports search, portal positioning and selected-check. Used at 1208 and 1375. Remove local export.\n• [medium] Sel — hand-built generic select/multiselect dropdown (portal, optional search, dividers, sub-labels, multi) (~lines 329-473) — status:handrolled; hand-rolled:dropdown\n  PLAN: Large bespoke portal dropdown re-implementing exactly what FormKit Dropdown/Select/MultiSelect provide (search, dividers, sub text, multi-select chips, reposition on scroll/resize). Replace with FormKit Select (single) / MultiSelect (multi) using the same options shape. Only in-file consumer is HijriDate (1146-1148); migrate that to FormKit Select or DateField then delete this local export.\n• [medium] Quote Issued success overlay (green-accent confirmation with quote no / iqama / total rows, copy buttons, Go-to-quote action) (~lines 1644-1701) — status:handrolled; hand-rolled:overlay, header, success, footer\n  PLAN: Own fixed overlay (rgba+blur, zIndex 2300), own success circle/check, custom row()/CopyBtn, custom footer button. Replace with FormKit SuccessScreen (open) or Modal `success` view: pass title 'تم إصدار التسعيرة', subtitle, and `rows`/`compareRows` for Quote No / Iqama / Total (SuccessView supports rows + code). Keep warning chips as children. Footer 'التسعيرة' nav -> ActionButton. Copy-to-clipboard buttons may need to remain custom inside the rows.\n• [high] HRSD (Ministry of Labor) inquiry overlay — multi-phase modal (loading / captcha image+input / verifying / error / result) (~lines 1703-1792) — status:handrolled; hand-rolled:overlay, header, fields, footer, success\n  PLAN: Fully custom fixed overlay (zIndex 2100) with its own header (Briefcase + title + X close), spinners, captcha image block, captcha TextField, error/result panels and action buttons. Wrap in FormKit Modal (title 'وزارة العمل', Icon=Briefcase, custom variant). Use Modal `pages` for the phase machine (loading/captcha/verifying/error/result) or render phase bodies as children; captcha input -> TextField; Verify/Retry/Skip/Continue -> ActionButton in Modal footer; the 'result found' summary -> SuccessView rows. Captcha image + CaptchaCountdown stay as custom body content. Note zIndex 2100/2300 are higher than FormKit Modal default so stacking over the wizard must be preserved.","src/pages/PaymentsPage.jsx":"• [high] PayModal — document/edit a transaction-fee payment (payment summary + bank account + reference + PDF receipt upload, with inline success screen) (~lines 802-993) — status:partial; hand-rolled:fields, sections, footer, success, fileupload\n  PLAN: Shell is correct (FKModal with title/Icon/accent/width/scroll/errorMsg/footer). Everything INSIDE is hand-rolled and should move to FormKit primitives: (1) The two bordered fieldsets with absolutely-positioned floating legends ('ملخص السداد' gold, 'تأكيد السداد' green) should be ModalSection/KCard with variant colors. (2) Read-only summary cells (Payment Type, Amount, SADAD No) are hand-built divs styled like inputs — replace with read-only TextField/CurrencyField or KCard read-only rows; the SADAD copy button can use FormKit's copy-enabled field/Flag pattern instead of the inline clipboard button + local sadadCopied state. (3) Bank account picker uses Sel from KafalaCalculator — swap to FormKit Select. (4) Payment reference uses a raw <input> (fS style object) — swap to FormKit TextField. (5) Receipt PDF upload is a fully hand-rolled drag/drop dropzone (receiptDrag state, label/htmlFor hidden <input type=file>, custom selected-file chip with remove button) — replace with FormKit FileField. (6) Footer is two hand-built <button>s (pm-nav-btn style block + custom close button) — replace with ActionButton; the saving spinner keyframe (pm-spin) is redundant with ActionButton loading state. (7) Success view is a hand-rolled checkmark-circle block gated by local `done` state — replace with FormKit Modal `success` prop / SuccessView/SuccessScreen. (8) Inline fS/lbl style objects and the manual required-asterisk spans should be dropped in favor of FormKit field labels/required handling. Validation (errorMsg) already flows through FKModal, keep it.","src/pages/SbcCenterPage.jsx":"• [high] AddModal — add-transaction multi-step wizard (name reserve / open CR / renew / amend / documents) (~lines 883-1245) — status:partial; hand-rolled:stepper/progress-bar, sections (fieldset+legend), all fields (text/number/file/segmented/select), footer nav buttons, error display, success\n  PLAN: Shell already uses FKModal (good). Replace the hand-rolled inner pieces: (1) the manual step progress bar (lines 1057-1061) and prev/next/submit footer (lines 1211-1241) should be replaced by FKModal's built-in `pages` array + `onSubmit`/`submitting`/`submitLabel`/`nextLabel`/`backLabel` props — the file currently manages `step`/`goNext` state by hand and renders its own `.nu-nav-btn` footer. (2) The `fieldset`/`legend` titled boxes (lines 1049-1050, 1066-1067, 1193-1194) → FormKit ModalSection/KCard. (3) Raw `<input style={inp}>` text/CR/reservation/sadad fields → FormKit TextField (with dir='ltr'/filter for monospace/numeric ones); the amount input → CurrencyField or NumberField. (4) `<Drop>` adapter (service type, company form, doc type, amend type, basic-field selects) → FormKit Select. (5) Language/entity-kind segmented `segBtn` button rows (1078-1081, 1089-1092) → FormKit Segmented. (6) file `<input type=file>`+button is only in non-wizard path indirectly; use FileField. (7) inline `errMsg` JSX → pass via pages[].error / errorMsg. (8) On success it navigates away (toast + onSaved + event) rather than showing a panel — optionally use FKModal `success={<SuccessView/>}`. Replace imported PersonFormModal `Dropdown`/`DateField`/`sF` with FormKit's Select/DateField/sF for true single-source.\n• [medium] BeneficiaryField search-results dropdown (person picker popover) — used in wizard 'parties' step and amend 'transfer' (~lines 694-741 (popover 726-736)) — status:handrolled; hand-rolled:dropdown (absolute-positioned floating result list via `dropWrap`), search input, segmented mode toggle, person chip\n  PLAN: The `position:absolute` result panel (dropWrap, lines 667/727-735) is a hand-built floating popover. Replace the whole picker with FormKit Dropdown (searchable, async-capable via renderCell) or wrap it as a Select; the بحث/إضافة-جديد toggle → Segmented; the inner name/id inputs → TextField/IdField; the selected PersonChip is custom presentation that can stay or move into Dropdown renderSelected. Currently re-implements debounced Supabase search + custom dropRow rendering that FormKit Dropdown already covers.\n• [medium] MuaqqibField search-results dropdown (muaqqib picker popover) — wizard 'parties' step (~lines 743-791 (popover 776-786)) — status:handrolled; hand-rolled:dropdown (absolute floating result list via `dropWrap`), search input, segmented mode toggle, custom +966 phone input, person chip\n  PLAN: Same pattern as BeneficiaryField. Floating result popover (776-786) → FormKit Dropdown (searchable, async). بحث/إضافة-جديد toggle → Segmented. The bespoke +966-prefixed phone input (765-769) → FormKit PhoneField. Name input → TextField. Selected chip via Dropdown renderSelected or keep PersonChip.\n• [medium] FacilityField search-results dropdown (facility picker popover, by unified/GOSI/HRSD) — used by CR-renew/amend/documents step (~lines 811-881 (popover 866-876)) — status:handrolled; hand-rolled:dropdown (absolute floating result list via `dropWrap`), search input, selected-facility detail card\n  PLAN: Floating result popover (866-876) → FormKit Dropdown (searchable, async, getSub for the matched-number label). The rich selected-facility card (840-862) showing unified/GOSI/HRSD + confirmation date is bespoke and can remain as a custom selected-state render (Dropdown renderSelected) or a KCard below the field. Replace the raw search `<input style={searchInpStyle(inp)}>` with the Dropdown's own search box.\n• [low] Drop adapter dropdown (generic single-select used throughout AddModal) (~lines 14-24) — status:partial; hand-rolled:dropdown\n  PLAN: Thin wrapper that already delegates to a canonical searchable `Dropdown` — but that Dropdown is imported from PersonFormModal, NOT from FormKit. Re-point the import to FormKit's `Dropdown`/`Select` so edits to FormKit propagate. The custom renderCell (gold check + label) can move to FormKit Select/Dropdown renderCell or be dropped in favor of FormKit's default selected styling.","src/pages/SbcFacilities.jsx":"• [low] Platform/view dropdown selector (table lens switcher: SBC/GOSI/Qiwa/Muqeem) (~lines 1761-1815) — status:handrolled; hand-rolled:dropdown\n  PLAN: _PlatformDropdown is a fully hand-rolled floating menu: a trigger <button> plus an absolutely-positioned (position:absolute, top:calc(100%+4px), zIndex:10, custom #161616 panel + boxShadow) list of <button> options, each with a brand dot, title, subtitle, and a checkmark on the active one. This is exactly FormKit's Dropdown / Select use case. Replace with FormKit Select (it natively supports value/onChange, options with label+sub, and active-state checkmark) or FormKit Dropdown for the raw floating panel. Map PLATFORMS -> options ([{v,l,sub,dotColor}]), tableView -> value, setTableView -> onChange; drop the local useState(open), the manual absolute panel, the inline #161616/boxShadow styling, and the hand-built chevron rotation. Use the Flag/dot affordance from FormKit if available, otherwise pass a custom option renderer.\n• [medium] Advanced-filter searchable selects (owner/manager/status/nitaq/partners/admins/sort) — hand-rolled Sel from KafalaCalculator (~lines 4233-4343 (Sel popups); panel container 4228-4360) — status:handrolled; hand-rolled:dropdown\n  PLAN: These eight <Sel> instances import a hand-rolled searchable/multiple select from ./KafalaCalculator.jsx (not FormKit). Each Sel renders its own floating option list with search box, multi-select chips, maxVisible truncation, and divider/section support. Swap every <Sel> for FormKit Select / MultiSelect: single-value sorts (sortConfirm, sortIssue) -> Select; multi-value filters (owner, manager, status, nitaq, partnersCount, adminsCount) -> MultiSelect with searchable + maxVisible. options/value/onChange/placeholder/searchable/searchPlaceholder props map almost 1:1; the {divider:true,l} section headers map to FormKit's option-group/divider syntax. Note: the enclosing advanced-search panel (line 4228) is an INLINE collapsible block, not a floating overlay, so it is NOT itself a popup — only the Sel option lists float. After migration, also drop the local advInp/advLbl style objects in favor of FormKit field styling.","src/pages/SyncHub.jsx":"• [medium] Person picker (RolePersonTabs 'Add person' overlay) (~lines 875-945) — status:handrolled; hand-rolled:overlay/header/fields/footer\n  PLAN: Replace the entire ReactDOM.createPortal block. Wrap in FormKit Modal (variant='add', icon=user-plus, title T('اختر شخصاً','Pick a person'), subtitle = the allowedRoles description string) — Modal already supplies overlay (rgba+blur), header (Icon+title+close), centered panel, dir, zIndex, and ScrollBox body, so drop the hand-rolled fixed overlay div, the inner #1a1a1a panel, the padded header div, and the close-X. Replace the hand-rolled <input> search box with FormKit TextField (or a search-styled TextField) bound to `search`. Render the candidate list inside the Modal body using KCard/ModalSection rows or a simple mapped list of FormKit list-row elements (keep the already/added disabled state and the ✓ added badge via Flag). Replace the footer Close <button> with ActionButton (secondary) calling setPickerOpen(false); Modal's footer handles layout. Keep openPicker/addTab/removeTab/filteredCands logic unchanged; just feed data into FormKit components. Tokens F/C should come from FormKit's F/C exports instead of the local const F and local C object.","src/pages/TransactionsPage.jsx":"• [medium] Date picker popup (DatePickerInput → CalendarPopup with fixed backdrop) (~lines 1916-1972) — status:handrolled; hand-rolled:calendar/overlay/fields\n  PLAN: DatePickerInput hand-rolls a masked text input (yyyy-mm-dd typing logic), a calendar toggle button, a fixed full-screen backdrop (div style position:fixed inset:0 zIndex:1000, line 1967) and anchors the imported CalendarPopup from KafalaCalculator.jsx (line 1968). Replace the whole component with FormKit DateField, which already bundles the masked input + FormKit CalendarPopup + outside-click dismissal. EditField type==='date' (line 1978-1979) currently delegates to DatePickerInput; route it to FormKit DateField instead. This removes the custom backdrop/anchor math entirely.\n• [high] Facility search results dropdown (Select Facility card) (~lines 1204-1238) — status:handrolled; hand-rolled:dropdown/fields\n  PLAN: Hand-rolled async-search combobox: custom search <input> (lines 1182-1191), custom scrollable results panel (div .tx-fac-results, maxHeight:320, overflowY:auto, lines 1205-1237) rendered inline under the input with per-row hover/selected styling and match-label badges. Replace with FormKit Select/MultiSelect configured for async remote search (option rows render name + UN/GOSI/HRSD), or build the floating list via FormKit Dropdown. The staged-vs-current selection state and SaveBtn can stay; only the input + results-panel chrome migrate to FormKit field + Dropdown.\n• [medium] Advanced filters panel (branch/status/date-range/clear) (~lines 525-541) — status:handrolled; hand-rolled:overlay/fields\n  PLAN: Inline expanding panel (advOpen toggle, not a true floating overlay) containing raw <select> (selS style) for branch + status, two native <input type=date>, and a hand-styled Clear button. Replace the selects with FormKit Select, the date inputs with FormKit DateField, and the Clear button with FormKit ActionButton (variant delete/ghost). Could optionally be lifted into a FormKit Modal/Dropdown filter sheet, but minimally swap the field primitives.\n• [medium] Qiwa subscription add-on inline form (checkbox-expanded SADAD + amount) (~lines 1846-1898) — status:handrolled; hand-rolled:fields/footer/confirm\n  PLAN: Inline (not floating) checkbox-gated mini-form: hand-rolled fake checkbox label (lines 1848-1856), raw SADAD text input, raw amount text input with manual decimal masking (lines 1858-1876), and a custom Confirm & Pay submit button (lines 1877-1895). Replace the checkbox with FormKit Checkbox/Switch, SADAD with FormKit IdField/TextField, amount with FormKit CurrencyField (drops the manual regex masking), and the submit button with FormKit ActionButton (variant add). The pending/paid badge (lines 1834-1844) can become a FormKit Flag.","src/pages/admin/AgentsPage.jsx":"• [medium] Agent edit modal (AgentEditModal) — edit form state (~lines 764-812 (component 667-814)) — status:partial; hand-rolled:fields, footer, sections (legend/fieldset), error display partially, select dropdowns\n  PLAN: Shell is correct (FKModal open/onClose/accent/width/scroll/title/Icon/errorMsg). Replace the custom `fieldset`+`legend` group with FormKit ModalSection/KCard. Replace raw inputs: name `<input>` -> TextField; id_number `<input>` (digits-only, 10) -> IdField; the hand-built +966 phone group (custom prefix box + manual 5X XXX XXXX formatting) -> PhoneField; default_commission_amount `<input>` (decimal, optional) -> CurrencyField (or NumberField); the two `Drop` selects (nationality, branch — imported from PermissionsPage, NOT FormKit) -> FormKit Select. Replace the custom `.cm-nav-btn` footer button + its inline <style> with ActionButton (variant edit) — FKModal renders it in the footer slot, so the inline style block goes away. Drop the local `lbl`/`inp`/`fieldset`/`legend`/`accent`/`reqStar` style objects (FormKit fields carry labels + required markers). Keep validation logic but let field components own formatting/maxLength.\n• [medium] Agent edit success/confirmation overlay (AgentEditModal `done` branch — 'تم تعديل البيانات بنجاح' with field change-diff list) (~lines 731-763) — status:handrolled; hand-rolled:overlay, header, close button, success checkmark, change-diff body\n  PLAN: Entirely custom: own fixed `overlay` (position:fixed, inset:0, blur, zIndex 1500) + `box` shell, own top-left close `<button>` with X icon, own 74px circular checkmark, own 'تم تعديل البيانات بنجاح' text, and a hand-built from->to change-diff list. Migrate to FormKit Modal's built-in `success` view (SuccessView/SuccessScreen) rather than early-returning a separate overlay. Render this as a `success` page of the same FKModal (pass success content/title) so overlay+close+checkmark come from FormKit; keep the change-diff rows as custom children inside the success body, or rebuild them with ModalSection rows. Removes the duplicate fixed-overlay/box/close styling entirely.","src/pages/admin/ClientsPage.jsx":"• [medium] Client edit modal (تعديل بيانات العميل) — edit client name/ID/phone/nationality/branches (~lines 711-854) — status:partial; hand-rolled:fields, footer, success, section (fieldset/legend), inputs (name/id/phone), validation styling\n  PLAN: This component renders in two distinct states and both need work.\n\n1) EDIT STATE (lines 808-852): Wrapped in FormKit <FKModal> (good shell — overlay/header/close/error are FormKit). But everything inside is hand-rolled:\n - The 'fieldset'/'legend' bordered group (styles lines 733-734, markup 818-822) → replace with FormKit ModalSection (or KCard) titled 'بيانات العميل'.\n - Name <input> (line 826) → replace with FormKit TextField (required).\n - ID number <input> with manual /\\D/ strip + slice(0,10) (line 830) → replace with FormKit IdField (handles 10-digit Saudi ID masking/validation).\n - Phone: hand-built +966 prefix box + manual digit grouping (lines 833-838) → replace with FormKit PhoneField.\n - Nationality: <Drop> from PermissionsPage (line 842) → replace with FormKit Select (options from nationalities).\n - Branch: <MultiDrop> from PermissionsPage (lines 847-848) → replace with FormKit MultiSelect.\n - 'reqStar' manual required asterisk (line 728) → use FormKit field `required` prop instead.\n - Local style objects 'box/overlay/fieldset/legend/lbl/inp' (lines 731-736) become dead once fields/section are FormKit.\n - Footer: hand-rolled <button className='cm-nav-btn'> with injected <style> tag (lines 811-817) → replace with FormKit ActionButton (variant edit), drop the inline <style>.\n\n2) SUCCESS STATE (lines 775-806): Fully hand-rolled overlay — its own fixed 'overlay' div + 'box' div, own close button (lines 780-783), own check-circle SVG, own diff/changes list. This bypasses FormKit entirely. → Replace with FormKit Modal `success` view / SuccessView (or Modal's built-in success prop). The before/after change rows are custom; render them inside the FormKit success body, or as a ModalSection within the success view. Remove the standalone overlay/box wrapper.\n\nAlso: lib-level — Drop/MultiDrop are hand-rolled dropdown popups imported from PermissionsPage.jsx; swapping to FormKit Select/MultiSelect removes that external dependency here.","src/pages/admin/PermissionsPage.jsx":"• [medium] WorkInfoModal — edit work fields (role, office, phone, email, password) (~lines 707-884) — status:partial; hand-rolled:fields/sections/footer/dropdown\n  PLAN: Shell is FKModal (good), but everything inside is hand-rolled. Replace the local `fieldset`/`legend` (lines 727-728, 835-839) with ModalSection/KCard. Replace raw `<input>` (inp style) for phone with PhoneField, email local-part with TextField (or a TextField with @jisr.com suffix), password with TextField + the suggest button as an adornment. Replace `Drop` (role) with FormKit Select and `MultiDrop` (branch_ids) with FormKit MultiSelect. Replace the custom footer button `.wm-nav-btn` (lines 829-832, 834) with FormKit ActionButton via Modal's onSubmit/submitLabel props (it already passes errorMsg). Drop the locally-defined F/C/accent/box/overlay constants in favor of FormKit tokens.\n• [medium] WorkInfoModal SUCCESS view — 'تم تعديل البيانات بنجاح' with from→to change rows (~lines 792-824) — status:handrolled; hand-rolled:overlay/header/success\n  PLAN: This success state is rendered as a completely separate hand-built panel (its own `overlay` fixed div + `box`, lines 794-795) that bypasses FKModal entirely, with a hand-rolled close button (797-800) and custom check-circle. Migrate to FormKit's built-in Modal `success` view (SuccessView/SuccessScreen) so it renders inside the same FKModal shell instead of a parallel overlay. The from→to diff rows can be passed as success content / a ModalSection list. Removes the duplicate overlay+box+close-button code.\n• [medium] NewUserModal — GM-only 2-step wizard to create a staff account (~lines 1051-1275) — status:partial; hand-rolled:fields/sections/dropdown\n  PLAN: Already uses FKModal with the `pages` multi-step API + onSubmit/submitLabel/nextLabel/backLabel and per-page valid/error — the shell, stepper, and footer nav are FormKit. Remaining hand-rolled parts are the page CONTENTS: local `fieldset`/`legend` → ModalSection; name_ar/name_en/id_number raw `<input>` → TextField/IdField; nationality & role `Drop` → Select; branch `MultiDrop` → MultiSelect; phone composite input → PhoneField; email local-part input → TextField (with @jisr.com suffix); password/password2 raw inputs → TextField with the 'اقتراح جديد' generate button as adornment. The Step2Init prefill effect (1039-1049) can stay. Drop local inp/lbl/fieldset/legend constants for tokens.\n• [medium] NewUserModal SUCCESS view — 'تم إنشاء الحساب بنجاح' with credentials (email/password) (~lines 1152-1183) — status:handrolled; hand-rolled:overlay/header/success\n  PLAN: Same anti-pattern as WorkInfoModal's success: a standalone hand-built `overlay`+`box` panel (1154-1155) outside FKModal, with its own close button (1157-1160) and check-circle. Migrate to FKModal's `success` view (SuccessView/SuccessScreen). The credential rows (name/email/password with CopyBtn) become success content; FormKit may also have a copy-row affordance. Eliminates the parallel overlay.\n• [medium] Drop — portal single-select dropdown (role, nationality) (~lines 891-946) — status:handrolled; hand-rolled:dropdown/overlay\n  PLAN: Fully custom ReactDOM.createPortal dropdown: own trigger button, own getBoundingClientRect positioning, own backdrop (z-index 1998) + floating list (z-index 1999, line 920), own search box and option rows. Replace entirely with FormKit Select (or the FormKit Dropdown primitive). Exported and reused by WorkInfoModal + NewUserModal, so a single swap to Select removes ~55 lines and propagates the design-system look everywhere it's used.\n• [high] MultiDrop — portal multi-select dropdown (offices/branch_ids) (~lines 953-1028) — status:handrolled; hand-rolled:dropdown/overlay\n  PLAN: Fully custom portal multi-select: own chip trigger (primary office + +N badge), own positioning, own backdrop + floating list (z 1998/1999, line 999), own search and checkbox rows with check icons. Replace with FormKit MultiSelect. Exported and used by both modals; if FormKit MultiSelect lacks the 'رئيسي/primary' chip + +N collapse behavior, extend MultiSelect in FormKit rather than keeping this local copy, so the behavior lives in the design system.","src/pages/portal/PortalApp.jsx":"• [low] Toast notification (fixed floating panel for success/error messages) (~lines 319) — status:handrolled; hand-rolled:overlay\n  PLAN: The Toast component is a position:fixed floating panel (zIndex 9999) rendered at line 65 and defined at line 319. FormKit has no dedicated toast export listed, but the app's canonical pattern should drive notifications through a shared mechanism rather than this bespoke fixed div. If FormKit/the design system has a toast/Flag utility, route through it; otherwise this is the one acceptable standalone but it should at minimum adopt FormKit tokens (F, C, R, SP) instead of the local F/C consts and hardcoded radius/padding so styling stays centralized.\n• [high] Login / OTP auth dialog (phone + 6-digit code entry card) (~lines 92-156) — status:handrolled; hand-rolled:overlay/header/fields/footer\n  PLAN: LoginPage renders a centered card (maxWidth 420, line 127) acting as a two-step auth dialog with hand-rolled multi-step logic (step 'phone' -> 'code'). Although it is a full-screen auth surface rather than a popup-over-page, it should be rebuilt on FormKit primitives: wrap the card body using ModalSection/KCard; replace the local Field helper (line 315) with FormKit field wrappers; replace the raw phone <input> (inputBig, line 136) with PhoneField; replace the OTP <input> (line 144) with a TextField/Stepper-style code field (or NumberField with maxLength); replace primaryBtn (line 314) with ActionButton; and model the phone->code two-step flow with FormKit Modal 'pages' (multi-page support) plus a success view via SuccessView/SuccessScreen if a confirmation screen is desired. Resend timer and 'change number' links become ActionButton variants. The whole auth experience could be presented inside a FormKit Modal shell so overlay/header/close are centralized.","src/pages/admin/roles/RoleUI.jsx":"• [medium] FacilityPicker — searchable facility-select dropdown (portal floating panel) (~lines 204-299) — status:handrolled; hand-rolled:dropdown/overlay/fields\n  PLAN: Entirely custom floating dropdown built with ReactDOM.createPortal to document.body, manual getBoundingClientRect positioning, flip-up logic, mousedown outside-click handler, a raw <input> search box (lines 264-268), and hand-styled option rows (lines 275-293) using the local sF style. Replace with FormKit's Select (or MultiSelect if multi) which already provides searchable options, portal positioning, and outside-click handling; map options to {value:o.id, label:o.name_ar} and surface cr_number via Select's option rendering/subtitle. The trigger button (sF-styled, lines 241-253) becomes Select's built-in trigger; the portal panel, search input, and option list are all dropped in favor of Select internals. If Select cannot render the two-line option (name_ar + CR number) or the >5-items conditional search, use FormKit's Dropdown primitive as the floating container instead of a bespoke portal.","src/pages/admin/roles/UserActivityCard.jsx":"• [medium] FilterSelect dropdown (filter-bar chip select for action/entity/period) (~lines 51-105) — status:handrolled; hand-rolled:dropdown/overlay/fields\n  PLAN: Replace this entire hand-built chip-button + absolutely-positioned option panel with FormKit's Dropdown (for the floating panel + outside-click handling) or, more idiomatically, FormKit Select. The component reimplements: open state, useRef + mousedown outside-click listener (lines 54-59), the trigger button (lines 64-76) and the absolutely-positioned option list with manual hover styling and z-index (lines 77-102). FormKit Select takes value/onChange/options directly and matches the design tokens; the gold-active styling can be expressed via Select props or the F/C/GOLD tokens. Drop the local F constant in favor of FormKit's F/C tokens.\n• [high] DetailPanel activity-detail slide-over drawer (portal overlay with diff viewer) (~lines 207-320) — status:handrolled; hand-rolled:overlay/header/fields/footer/close-button/portal\n  PLAN: Entirely custom slide-over built with ReactDOM.createPortal (line 225), a hand-rolled fixed overlay with backdrop blur and click-to-close (lines 226-229), an injected <style> keyframe for slide-in animation (line 237), a custom header with icon-tile + title + hand-built close button (lines 238-265), and a custom scroll body (line 267). Migrate to FormKit Modal: pass Icon+title to Modal's header (removes lines 238-265 incl. the X close button), use Modal's overlay/animation/portal (removes lines 225-237), and wrap the body content in ModalSection/KCard. The InfoBox grid (lines 278-285, helper at 322-336) maps to a FormKit ModalSection of read-only key/value rows. The DiffRow before/after diff table (helper 178-205, rendered 287-308) is bespoke and has no direct FormKit field; keep it as custom content rendered inside a FormKit ModalSection (FormKit has no diff component). Note Modal is centered by default whereas this is a side drawer anchored to the inline-start edge (justifyContent flex-start) — confirm FormKit Modal supports a side/drawer variant or accept the centered layout. ScrollBox can replace the manual overflowY scroll body.","src/pages/admin/roles/UserRolePage.jsx":"• [high] User profile link/edit form (UserModal) (~lines 250-333) — status:handrolled; hand-rolled:overlay/header/fields/footer/dropdown\n  PLAN: Shell uses ModalShell + SaveBtn from RoleUI.jsx, NOT FormKit Modal. Replace ModalShell with FormKit Modal (variant 'edit'/'add', Icon=User, title). Replace footer SaveBtn with FormKit ActionButton. Replace local KCard/Lbl with FormKit ModalSection (or FormKit KCard). Fields: replace raw <input> for name_ar/name_en with FormKit TextField (name_en with uppercase transform). Replace the hand-built +966 phone group (lines 260-283) with FormKit PhoneField (which already handles the +966 prefix and 9-digit normalization). Replace the non-FormKit Dropdown for preferred_lang with FormKit Select. Replace the hand-built role-chip checkbox toggles (lines 291-313) with FormKit MultiSelect (or FormKit Segmented/Checkbox group). Replace the custom 'is_active' circular checkbox (lines 315-329) with FormKit Switch or YesNo. Drive footer disabled/saving state via ActionButton loading prop.\n• [medium] Save success overlay (تم التعديل / تم الربط) (~lines 206-248) — status:handrolled; hand-rolled:overlay/success\n  PLAN: Entirely custom ReactDOM.createPortal fixed overlay (position:fixed inset:0, backdrop blur) with bespoke @keyframes pop/check/fade animation and inline SVG checkmark. Replace with FormKit Modal's built-in 'success' view (pass success state/config to the Modal) or FormKit SuccessView / SuccessScreen, passing the title (isEdit ? 'تم التعديل' : 'تم الربط') and subtitle (f.name_ar). This removes the portal, the custom <style> keyframes block, and the hand-drawn check SVG. Once the form is migrated to FormKit Modal, fold this success state into that same Modal's success rendering rather than a separate portal.","src/pages/branch/BranchRentCard.jsx":"• [high] ContractModal — rent contract add/edit 2-step wizard (fields + file upload + auto-schedule) (~lines 220-520) — status:handrolled; hand-rolled:overlay/header/fields/footer/success/datepicker/fileupload/stepper/select\n  PLAN: Entirely custom. Replace the ReactDOM.createPortal + fixed overlay div (317-318) and the embedded <style> block (319-333) with FormKit Modal using variant='create'/'edit' and the multi-page 'pages' prop for the 2 steps; drop the manual step-progress bars (354-358) since Modal handles page indicators. Header (346-353, Building2 icon + title) -> Modal's Icon+title props. Replace hand-rolled close button (338-343) with Modal's built-in close. Step 1 fields: vendor/account_no -> TextField; amount input with grpNum/rawNum (398) -> CurrencyField (it provides live thousands grouping, removing grpNum/rawNum helpers); start_date/end_date DateField (imported from KafalaCalculator) -> FormKit DateField. Step 2: NiceSel frequency (406) -> FormKit Select; the dashed file dropzone + selected-file chip + remove (419-446) -> FormKit FileField; the editable schedule rows (458-470, custom DateField + raw amount input + Trash2 delete) -> repeated FormKit DateField + CurrencyField rows inside a ModalSection/KCard (the titled gold-bordered box at 389-393 -> ModalSection). The custom success screen (364-387, cmPop/cmFade animated check + summary tiles) -> FormKit Modal 'success' view / SuccessView with summary rows. Footer prev/next/save buttons (488-513, .cm-next/.cm-back) -> ActionButton in Modal footer / pages nav. Inline footer error (496-499) -> Modal's error handling. Remove local inp/lbl/fS/fLtr/lblS style objects in favor of FormKit tokens (F, C, TS, R).\n• [medium] PaymentModal — add single schedule payment (due date, amount, notes) (~lines 522-547) — status:partial; hand-rolled:overlay/header/footer/fields(via ObModal+raw inputs)\n  PLAN: Uses the local ObModal/Field/SaveBtn shell from ObligationModalUI.jsx (a parallel non-FormKit modal kit), not FormKit. Replace <ObModal> wrapper (537) with FormKit Modal (variant='add', Icon=Wallet, title='إضافة دفعة', single ModalSection labeled 'بيانات الدفعة'). Replace the three Field+raw <input> rows (541-543): due_date native date input (fDate) -> FormKit DateField; amount type=number (fMono) -> CurrencyField or NumberField; notes input (fText) -> TextField or TextArea. Replace SaveBtn footer (539) with ActionButton (variant add). Drop imports of ObModal/Field/SaveBtn/NiceSel/fText/fMono/fDate from ObligationModalUI.jsx once both modals migrate.\n• [low] Delete-payment confirm — native window.confirm() before deleting a schedule row (~lines 94-99) — status:handrolled; hand-rolled:confirm\n  PLAN: delPayment uses the browser-native confirm('حذف هذه الدفعة؟') (line 95). Replace with FormKit ConfirmDialog (variant='delete') gated by a small piece of state (e.g. confirmDel holding the payment), invoking the existing delete supabase call on confirm. This brings the destructive-delete prompt into the design system instead of a native OS dialog.","src/pages/branch/BranchObligationsCard.jsx":"• [high] ObligationModal — add/edit obligation 2-step wizard (item data + file/schedule) (~lines 199-485) — status:handrolled; hand-rolled:overlay/header/fields/footer/success/calendar/dropdown\n  PLAN: Entirely custom: ReactDOM.createPortal with a hand-built fixed overlay (lines 277-279), absolute close button (297-302), icon+title header with manual step-progress bars (304-319), and a hand-rolled <style> block (280-293) for .cm-next/.cm-back nav buttons and .cm-nospin inputs. Replace the whole shell with FormKit Modal using variant='add'/'edit', Icon={Receipt}, title, and the built-in multi-page 'pages' prop (FormKit Modal supports multi-page) to drive step1/step2 — removing the manual step bar and the .cm-next/.cm-back footer nav (ActionButton handles next/back/save). Step-1 grid (354-367): swap NiceSel for FormKit Select (النوع/الدورية), native <input> vendor/account/notes for TextField, the grouped-amount text input (363) for CurrencyField (it already does live thousands grouping, dropping grpNum/rawNum helpers), and the imported DateField (from KafalaCalculator) for FormKit DateField/CalendarPopup (start_date/end_date, 364-365). Step-2 (368-446): replace the hand-built drag-drop file <label>/<input> + uploaded-file chip (378-405) with FormKit FileField; wrap the schedule editor in a FormKit ModalSection/KCard (replacing the absolute-positioned bordered fieldset at 349-353); each schedule row's DateField+amount input (423-435) becomes FormKit DateField + CurrencyField; the delete-row button and total row stay as light custom markup or use ActionButton. Replace the hand-built success screen (323-347) — custom SVG check + summary rows — with FormKit Modal 'success' view / SuccessView passing the summary key/value list. Move the footer error banner (461-464) to FormKit's error handling. Inline input styles (fS/fLtr/lblS, 267-270) and the C color map are then dropped in favor of FormKit tokens (F, C, TS, FW).\n• [medium] PaymentModal — add a single payment to an obligation (~lines 487-509) — status:partial; hand-rolled:overlay/header/fields/footer\n  PLAN: Uses ObModal/Field/SaveBtn imported from the local './ObligationModalUI.jsx' module (line 5) — NOT FormKit. ObModal is a non-FormKit modal shell, and the three inputs are raw native <input> (type='date', type='number', text) styled with fDate/fMono/fText from the same local module (503-505). Migrate the shell: replace ObModal with FormKit Modal (variant='add', Icon={Wallet}, title='إضافة دفعة', and a ModalSection labeled 'بيانات الدفعة' with SectionIcon analog). Replace the Field wrappers + native inputs with FormKit fields: due_date -> DateField, amount -> CurrencyField, notes -> TextField. Replace SaveBtn in the footer with FormKit ActionButton (label 'إضافة الدفعة', disabled while saving). This also removes the dependency on ObligationModalUI.jsx for this modal.","src/pages/branch/ObligationModalUI.jsx":"• [medium] DatePick — hand-built popup calendar (portaled gold month-grid date picker) (~lines 25-85) — status:handrolled; hand-rolled:calendar/dropdown/overlay\n  PLAN: Replace entirely with FormKit DateField (or the standalone CalendarPopup export from FormKit). This component re-implements the whole picker: a custom gradient trigger button (lines 62-66), manual ReactDOM.createPortal popover positioned via getBoundingClientRect with resize/scroll listeners (lines 32-48, 67-82), custom outside-click handling, and a hand-built AR_MONTHS/AR_DAYS month grid (lines 49-79). FormKit's DateField/CalendarPopup already provides portaled positioning, outside-click, and the gold-accented Arabic month grid. Swap call sites from <DatePick value onChange/> to FormKit <DateField/>. The local AR_MONTHS/AR_DAYS constants become dead code.\n• [medium] NiceSel — hand-built dropdown (portaled gold-accented option panel) (~lines 122-176) — status:handrolled; hand-rolled:dropdown/overlay\n  PLAN: Replace with FormKit Select (or FormKit Dropdown for the raw panel). NiceSel re-implements the whole select: custom gradient trigger button (lines 150-154), manual ReactDOM.createPortal panel with up/down placement math via getBoundingClientRect and resize/scroll listeners (lines 135-147, 155-173), custom mousedown + Escape outside-close (lines 128-134), and hand-rendered option rows with hover/selected styling + check icon (lines 158-169). FormKit Select already covers trigger + portaled gold panel + keyboard close. Swap call sites from <NiceSel value onChange options/> to FormKit <Select options={...}/> (option shape {value,label} maps to FormKit). Note SelField (lines 105-118) is a separate, simpler native <select> wrapper — not a floating panel itself, but it too should migrate to FormKit Select."}